[project]
name = "flight-tracker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyopensky>=2.7",
    "FlightRadarAPI>=1.3",
    "pandas>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
