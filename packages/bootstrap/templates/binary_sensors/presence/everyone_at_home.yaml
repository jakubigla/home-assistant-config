---
binary_sensor:
  - name: presence_everyone_at_home
    device_class: presence
    state: >
      {{ is_state(
        "person.jakub", "home"
      ) and is_state(
        "person.sona", "home"
      ) }}
