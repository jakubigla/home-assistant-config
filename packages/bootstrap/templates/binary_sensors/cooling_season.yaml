---
binary_sensor:
  - name: cooling_season
    state: >
      {{ now().month in [5, 6, 7, 8, 9] }}
    icon: >
      {{ 'mdi:snowflake' if is_state('binary_sensor.cooling_season', 'on') else 'mdi:radiator' }}
