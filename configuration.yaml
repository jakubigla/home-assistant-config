---
homeassistant:
  packages:
    bootstrap: !include_dir_merge_named packages/bootstrap
    frontend: !include_dir_merge_named packages/frontend
    stairway: !include_dir_merge_named packages/stairway

# Let's keep these, so we can do some simple development in UI
# before we create a proper configuration via code
automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml
